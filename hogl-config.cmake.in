get_filename_component(HOGL_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" DIRECTORY)

add_library(hogl INTERFACE IMPORTED)
target_include_directories(hogl INTERFACE "@includedir@")
target_link_libraries(hogl INTERFACE @libdir@/libhogl.so pthread rt ffi)

set(HOGL_VERSION "@VERSION@")
set(HOGL_LIBRARIES hogl)

message(STATUS "HOGL version: ${HOGL_VERSION}   : ${HOGL_CMAKE_DIR}")
